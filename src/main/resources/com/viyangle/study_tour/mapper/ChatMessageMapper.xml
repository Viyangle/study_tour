<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.viyangle.study_tour.mapper.ChatMessageMapper">

    <insert id="insert" parameterType="com.viyangle.study_tour.pojo.ChatMessage"
            useGeneratedKeys="true" keyProperty="id">
        insert into chat_messages
        (session_id, sender_account_id, content, msg_type)
        values
        (#{sessionId}, #{senderAccountId}, #{content}, #{msgType})
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        delete from chat_messages where id = #{id}
    </delete>

    <update id="updateById" parameterType="com.viyangle.study_tour.pojo.ChatMessage">
        update chat_messages
        set session_id = #{sessionId},
            sender_account_id = #{senderAccountId},
            content = #{content},
            msg_type = #{msgType}
        where id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultType="com.viyangle.study_tour.pojo.ChatMessage">
        select id, session_id, sender_account_id, content, msg_type, sent_at
        from chat_messages
        where id = #{id}
    </select>

    <select id="selectAll" resultType="com.viyangle.study_tour.pojo.ChatMessage">
        select id, session_id, sender_account_id, content, msg_type, sent_at
        from chat_messages
    </select>

</mapper>
