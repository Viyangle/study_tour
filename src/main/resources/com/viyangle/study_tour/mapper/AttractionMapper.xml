<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.viyangle.study_tour.mapper.AttractionMapper">

    <sql id="attractionColumns">
        poi_id, parent_poi_id, name, address, location, pcode, pname, citycode, cityname, adcode, adname, type, typecode, distance, opentime_today, opentime_week, tel, status, created_at, updated_at
    </sql>

    <sql id="activeStatusCondition">
        (status is null or status = 'ACTIVE')
    </sql>

    <insert id="insert" parameterType="com.viyangle.study_tour.pojo.Attraction">
        insert into attractions
        (poi_id, parent_poi_id, name, address, location, pcode, pname, citycode, cityname, adcode, adname, type, typecode, distance, opentime_today, opentime_week, tel, status)
        values
        (#{poiId}, #{parentPoiId}, #{name}, #{address}, #{location}, #{pcode}, #{pname}, #{citycode}, #{cityname}, #{adcode}, #{adname}, #{type}, #{typecode}, #{distance}, #{opentimeToday}, #{opentimeWeek}, #{tel}, coalesce(#{status}, 'ACTIVE'))
    </insert>

    <delete id="deleteByPoiId" parameterType="java.lang.String">
        delete from attractions where poi_id = #{poiId}
    </delete>

    <update id="updateByPoiId" parameterType="com.viyangle.study_tour.pojo.Attraction">
        update attractions
        set parent_poi_id = #{parentPoiId},
            name = #{name},
            address = #{address},
            location = #{location},
            pcode = #{pcode},
            pname = #{pname},
            citycode = #{citycode},
            cityname = #{cityname},
            adcode = #{adcode},
            adname = #{adname},
            type = #{type},
            typecode = #{typecode},
            distance = #{distance},
            opentime_today = #{opentimeToday},
            opentime_week = #{opentimeWeek},
            tel = #{tel},
            status = coalesce(#{status}, status)
        where poi_id = #{poiId}
    </update>

    <insert id="upsert" parameterType="com.viyangle.study_tour.pojo.Attraction">
        insert into attractions
        (poi_id, parent_poi_id, name, address, location, pcode, pname, citycode, cityname, adcode, adname, type, typecode, distance, opentime_today, opentime_week, tel, status)
        values
        (#{poiId}, #{parentPoiId}, #{name}, #{address}, #{location}, #{pcode}, #{pname}, #{citycode}, #{cityname}, #{adcode}, #{adname}, #{type}, #{typecode}, #{distance}, #{opentimeToday}, #{opentimeWeek}, #{tel}, coalesce(#{status}, 'ACTIVE'))
        on duplicate key update
        parent_poi_id = IF(VALUES(parent_poi_id) IS NULL OR VALUES(parent_poi_id) = '', parent_poi_id, VALUES(parent_poi_id)),
        name = IF(VALUES(name) IS NULL OR VALUES(name) = '', name, VALUES(name)),
        address = IF(VALUES(address) IS NULL OR VALUES(address) = '', address, VALUES(address)),
        location = IF(VALUES(location) IS NULL OR VALUES(location) = '', location, VALUES(location)),
        pcode = IF(VALUES(pcode) IS NULL OR VALUES(pcode) = '', pcode, VALUES(pcode)),
        pname = IF(VALUES(pname) IS NULL OR VALUES(pname) = '', pname, VALUES(pname)),
        citycode = IF(VALUES(citycode) IS NULL OR VALUES(citycode) = '', citycode, VALUES(citycode)),
        cityname = IF(VALUES(cityname) IS NULL OR VALUES(cityname) = '', cityname, VALUES(cityname)),
        adcode = IF(VALUES(adcode) IS NULL OR VALUES(adcode) = '', adcode, VALUES(adcode)),
        adname = IF(VALUES(adname) IS NULL OR VALUES(adname) = '', adname, VALUES(adname)),
        type = IF(VALUES(type) IS NULL OR VALUES(type) = '', type, VALUES(type)),
        typecode = IF(VALUES(typecode) IS NULL OR VALUES(typecode) = '', typecode, VALUES(typecode)),
        distance = IF(VALUES(distance) IS NULL OR VALUES(distance) = '', distance, VALUES(distance)),
        opentime_today = IF(VALUES(opentime_today) IS NULL OR VALUES(opentime_today) = '', opentime_today, VALUES(opentime_today)),
        opentime_week = IF(VALUES(opentime_week) IS NULL OR VALUES(opentime_week) = '', opentime_week, VALUES(opentime_week)),
        tel = IF(VALUES(tel) IS NULL OR VALUES(tel) = '', tel, VALUES(tel)),
        status = IF(VALUES(status) IS NULL OR VALUES(status) = '', status, VALUES(status))
    </insert>

    <select id="selectByPoiId" parameterType="java.lang.String" resultType="com.viyangle.study_tour.pojo.Attraction">
        select <include refid="attractionColumns"/>
        from attractions
        where poi_id = #{poiId}
    </select>

    <select id="selectByPoiIds" resultType="com.viyangle.study_tour.pojo.Attraction">
        select <include refid="attractionColumns"/>
        from attractions
        where poi_id in
        <foreach collection="poiIds" item="poiId" open="(" separator="," close=")">
            #{poiId}
        </foreach>
    </select>

    <select id="selectActiveByPoiIds" resultType="com.viyangle.study_tour.pojo.Attraction">
        select <include refid="attractionColumns"/>
        from attractions
        where <include refid="activeStatusCondition"/>
          and poi_id in
        <foreach collection="poiIds" item="poiId" open="(" separator="," close=")">
            #{poiId}
        </foreach>
    </select>

    <select id="selectAll" resultType="com.viyangle.study_tour.pojo.Attraction">
        select <include refid="attractionColumns"/>
        from attractions
    </select>

    <select id="selectAllActive" resultType="com.viyangle.study_tour.pojo.Attraction">
        select <include refid="attractionColumns"/>
        from attractions
        where <include refid="activeStatusCondition"/>
    </select>

    <select id="selectByRegionCode" resultType="com.viyangle.study_tour.pojo.Attraction">
        select <include refid="attractionColumns"/>
        from attractions
        <where>
            <if test="regionCode != null and regionCode != ''">
                and (adcode = #{regionCode} or left(adcode, 4) = left(#{regionCode}, 4))
            </if>
        </where>
        order by updated_at desc, created_at desc
    </select>

</mapper>
